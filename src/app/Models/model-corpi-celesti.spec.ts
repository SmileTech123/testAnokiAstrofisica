import { ModelCorpiCelesti } from './model-corpi-celesti';

describe('ModelCorpiCelesti', () => {
  it('should create an instance', () => {
    expect(new ModelCorpiCelesti()).toBeTruthy();
  });
});
